### 新增功能

1. **串口热插拔自动刷新** - 插入/拔出串口后侧边栏列表 2 秒内自动更新，无需手动刷新；虚拟串口（com0com）的新增/删除同样自动感知。
2. **COM 分组常驻显示** - 修复未插入串口启动应用时 COM 分组整体隐藏、无刷新入口的问题；分组始终可见，空列表显示空态提示，手动刷新按钮保留作为兜底。

### 优化修复

1. **轮询零占用优化** - 热插拔检测采用 2 秒轮询（仅变化时通知界面，平时完全静默）；Windows 注册表串口扫描改为异步执行，不再阻塞主进程。
2. **发布产物命名统一** - Release 资产命名统一为 `名称-版本-系统-架构-变体` 格式，按系统自动分组排列，查找下载更直观；macOS dmg 命名补充版本号，修复 latest-mac.yml 版本号提取失败的问题。
3. **CI 产物拆分** - CI 构建产物按安装包拆分为独立 artifact，下载即为单独的包。

### 包含上游 v1.2.5 全部更新

虚拟串口模拟（com0com）、com0com 一键安装、命令循环批量运行、多实例支持、注册表扫描补全串口、断开后日志立即落盘、HEX 发送修复、发送后清空发送区等。

### 提交记录

- `e403da0` perf: Windows 注册表串口扫描改为异步 execFile
- `f53ecd0` feat: 串口热插拔自动刷新 + COM 分组常驻显示
- `12e5dd2` ci: CI 产物按安装包拆分为独立 artifact
- `32079f0` ci: 统一发布产物命名，Release 资产按系统分组排序
- `7bc3b45` 合并上游 v1.2.5 更新
