### 新增功能（同步上游 v1.2.5）

1. **虚拟串口模拟** - 新增虚拟串口模拟功能（Windows），支持检测 com0com 是否安装、显示虚拟串口对列表、新增/删除串口对、修改串口配置（串口号、隐藏、模拟波特率、缓冲区溢出、误码率、RTTO、RITO、模拟插拔、独占模式等），表格中显示绑定串口对应关系。
2. **com0com 一键安装** - 检测到 com0com 未安装时显示"点此安装"按钮，点击后运行内置的 setup.exe 安装程序，简化首次使用体验。
3. **命令循环批量运行** - 命令编辑支持循环批量运行，可自动重复发送命令序列。
4. **多实例支持** - 支持应用多实例运行。

### 优化修复（同步上游 v1.2.5）

1. **串口扫描增强** - 扫描串口时支持从注册表扫描，补全串口列表。
2. **断开后日志刷新** - 点击断开连接后立即将日志刷新到文件，避免数据丢失。
3. **HEX 发送修复** - 修复 HEX 模式发送数据的问题，接收 HEX 数据统一转大写。
4. **发送后清空** - 支持发送命令后自动清空发送区。
5. **连接中图标动画** - 连接中的图标添加动画效果，连接状态更直观。
6. **菜单栏关闭优化** - 左下角设置菜单栏点击任意菜单后自动关闭。
7. **快捷键恢复默认修复** - 修复快捷键恢复默认按钮文字被截断显示的问题。
8. **编辑连接标题修复** - 修复编辑连接时标题显示问题。
9. **Tooltip 显示优化** - 限制最大宽度避免撑开窗口，统一 tooltip 悬浮延迟为 500ms。

### Fork 适配

1. **useTerminal API 兼容** - 保留 fork 的 `saveLogFileAs` / `rotateLogFile` 接口，同时兼容上游 `saveLogFile` 别名，合并后功能无差异。

### 提交记录

- `7bc3b45` 合并上游 v1.2.5 更新（虚拟串口模拟、多实例、注册表扫描等 32 个提交）
- 上游基线：`827cbbd` 更新版本为1.2.5
