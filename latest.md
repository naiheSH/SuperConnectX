### 新增功能

1. **串口备注** - 串口右键菜单可添加备注，备注更新立即生效，串口名悬浮 tooltip 显示备注，日志导出文件名自动使用最新备注。
2. **跨平台 CI 构建** - 新增 GitHub Actions 跨平台构建工作流，覆盖 Windows、Linux、macOS。
3. **macOS 双架构发布** - 发布流程同时输出 arm64 和 x64 安装包。
4. **Ubuntu 24 兼容构建** - 新增基于 Electron 35 的 Ubuntu 24 兼容包，与默认 Electron 38 版本同时发布。
5. **命令组 HEX 格式支持（上游 v1.2.4）** - 命令组中的命令支持 HEX 格式，方便发送十六进制数据。

### 优化修复

1. **Ctrl+F 搜索框修复** - 修复搜索框不显示的问题，搜索窗口移至左上角并确保显示在最上层。
2. **导出性能优化** - 优化大文件导出性能，修复导出卡顿问题。
3. **Ubuntu 窗口卡顿优化** - 优化 Ubuntu 下窗口操作卡顿。
4. **Linux 打包修复** - 修复 chrome-sandbox 权限与串口原生模块加载问题。
5. **Linux 终端字体优化** - 终端字体链按平台适配，修复 Linux 下中西文宽度失调导致显示效果差的问题（已贡献回上游）。
6. **历史记录弹框显示优化**。
7. **禁止自动发起网络请求**。

### 上游同步（v1.2.2 ~ v1.2.4）

1. **Windows 虚拟串口修复** - 修复 Windows 下虚拟串口不显示的问题。
2. **滚动固定优化** - 点击按钮滚动到最底部时，自动取消屏幕固定。
3. **GB2312 日志修复** - 修复切换 GB2312 编码之后日志不显示的问题。
4. **复制内容修复** - 从输出中复制时替换 `\0` 字符避免截断；修复终端 Ctrl+C 复制不生效的问题。
5. **选项卡关闭提示** - 鼠标悬浮到选项卡关闭按钮时弹出固定提示。
6. **红色按钮前景色优化** - 红色按钮的前景色统一为白色，提升可读性。

### 提交记录

- `cf4f29c` 合并上游 v1.2.4 更新
- `bd14b9e` feat: 串口右键菜单添加备注
- `f8df682` fix: 修复 ctrl+F 搜索框不显示问题
- `974c7db` perf: 优化大文件导出性能
- `4758785` fix: 修复导出卡顿问题
- `113356e` perf: 优化 Ubuntu 窗口卡顿
- `f71a52c` CI: 新增跨平台构建工作流（Windows/Linux/macOS 双架构）
- `9311762` feat: 发布流程支持 macOS 双架构（arm64 + x64）
- `a800b12` feat: 添加 Ubuntu 24 兼容构建（Electron 35）
- `e53e470` fix: 修复 Linux 打包的 chrome-sandbox 权限与串口原生模块加载
- `7a082c8` fix: 串口列表按平台过滤虚拟端口，修复测试跨平台兼容性
- `11c9a0b` fix: 终端字体链按平台适配，修复 Linux 显示效果差
- `2ee93cb` 合并上游 v1.2.3 更新
- `349ed2f` 合并上游 v1.2.2 更新
