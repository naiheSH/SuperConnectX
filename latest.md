# v1.2.8-naihe1

### 个人版功能与行为
1. **日志另存为** - 工具栏按钮明确为“另存为”，调用 `saveLogFileAs` 按当前导出设置导出日志；内部 `rotateLogFile` 保留但当前无 UI 入口
2. **日志管理** - 支持分片开关、分片大小和不限制选项，并明确按时间导出与清理规则

### 新增功能
1. **会话布局恢复** - 重启应用后自动恢复上次打开的选项卡、固定状态、活动选项卡和分屏布局，无缝续接上次工作现场
2. **串口友好名称与设备详情** - 串口列表支持显示串口友好名称与设备详情，悬停可查看详情、支持按设备信息搜索，并提供独立显示开关
3. **方向键切换历史命令** - 命令输入框支持用上/下方向键快速切换历史发送命令
4. **Telnet 多字符编码支持** - Telnet 连接同步支持多字符编码

### 优化修复
1. **组件模板化重构** - 大规模架构重构：抽取通用基础组件模板（窗口标题栏、侧边栏、状态栏、设置布局、消息通知、分屏与拖拽等），拆分日志模块，存储、备份、升级、托盘等主进程能力迁移到独立核心模块，大量代码精简复用并补充完善单元测试
2. **Linux 串口权限修复与打包完善** - deb 安装后自动修复 chrome-sandbox 权限并写入覆盖 ttyUSB/ttyACM/ttyAMA/rfcomm/ttyS 的 udev 规则，卸载时自动清理；snap 声明 serial-port plug（安装后需手工连接）；AppImage 打开串口遇到 EACCES 时可通过 pkexec 一键修复权限，并提供结构化错误提示
3. **接收缓冲区修复** - 修复无换行数据流下接收缓冲区无限增长的问题
4. **日志打开失败修复** - 修复断开连接后点击"打开所在文件夹/打开日志"提示打开失败的问题
