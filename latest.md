# v1.2.9-naihe1

### 个人版功能与行为
1. **发布与更新源** - 个人版发布包和自动更新使用 [naiheSH/SuperConnectX](https://github.com/naiheSH/SuperConnectX)，不跟踪上游 GitHub Releases；`naihe` 版本号按普通 Release 处理，不会被当成预发布通道
2. **日志另存为与归档** - 日志另存为入口在文件菜单（`Ctrl+Shift+S`，支持自定义改绑），工具栏为「日志归档」

### 新增功能
1. **日志清理体系与存储健壮性** - 支持按最大保留天数和最大文件数自动清理日志（0 为不限制），设置页可手动触发立即清理并区分成功/失败提示；写入前检查磁盘可用空间，空间不足时告警并停止写入；写入失败进入重试队列定时重放；日志分片开关与大小设置实时生效
2. **日志另存为与快捷键** - 日志另存为支持按时间范围导出（全部/最近 N 小时）并带导出进度反馈，入口移至文件菜单并新增快捷键 `Ctrl+Shift+S`（支持自定义改绑即时生效），保存对话框自动记忆上次目录；工具栏恢复「日志归档」按钮
3. **通知显示时长设置** - 通知支持自定义显示时长（永久显示/5 秒等），重复通知刷新计数时同步重置倒计时
4. **主侧栏与底部面板切换** - 新增主侧栏与底部面板显示切换并持久化

### 优化修复
1. **组件模块化重构** - 大规模架构重构：按功能抽取到 `features`（命令、连接、诊断、快捷键、选项卡、终端、虚拟串口），新增 `foundation` 基础层，大量代码精简复用并补充完善单元测试
2. **连接启动与重连竞态修复** - 主进程 `DirectConnector`/`WorkerPool` 串行化连接启动与停止，渲染进程引入连接代际机制与重试定时器管理，关闭/重连/销毁时取消挂起重试并清理过期监听器，杜绝过期连接的数据与关闭事件串扰新连接
3. **串口备注竞态修复** - 引入请求防竞态避免快速切换选项卡/串口时旧请求覆盖新备注，缓存判断改用 `Object.hasOwn`，新增串口列表右键菜单直接编辑备注并同步刷新所有已打开终端
4. **窗口交互与资源释放修复** - 窗口最大化状态改用类型化 IPC 通知并做状态去重、增加 resize 兜底兼容 Ubuntu 24；修复 macOS 窗口交互与串口资源释放问题
5. **串口名称显示优化** - 优化跨平台串口名称显示、修复显示宽度并保留 macOS 串口访问模式标识
6. **界面布局修复** - 修复分屏与终端底部面板状态：恢复标签拖拽创建分屏的目标区域适配，消除连接、关闭和窗口缩放时底部面板的瞬时跳动，每个终端标签独立保存底部面板高度
7. **其他** - 通知默认改为常驻显示；补充 macOS 未签名安装与更新指引文档；修复 macOS 个人版更新检查
