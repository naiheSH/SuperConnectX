### 新增功能

1. **日志过滤功能** - 新增日志过滤功能，支持按条件筛选日志内容，并优化筛选显示效果，方便快速定位关键信息。
2. **自定义发送/接收前缀** - 支持自定义发送前缀和接收前缀，适配不同协议与设备的数据格式。
3. **串口热插拔自动刷新** - 插入/拔出串口后侧边栏列表 2 秒内自动更新，无需手动刷新；虚拟串口（com0com）的新增/删除同样自动感知。
4. **COM 分组常驻显示** - 修复未插入串口启动应用时 COM 分组整体隐藏、无刷新入口的问题；分组始终可见，空列表显示空态提示，手动刷新按钮保留作为兜底。
5. **立即备份按钮** - 新增立即备份按钮，可随时一键执行手动备份。

### 优化修复

1. **日志空文件修复** - 修复日志目录下产生大量空文件的问题。
2. **串口扫描性能优化** - Windows 注册表串口扫描改为异步 `execFile`，提升扫描效率与响应速度。
3. **编码设置调整** - 将编码选项从更多串口设置中移出，设置入口更清晰。
4. **macOS 图标修复** - 修复 macOS arm 架构下图标显示为 Electron 默认图标的问题。
5. **CI/发布优化** - 添加 sha256 校验文件发布、CI 构建缓存加速、补充并修复 E2E 测试用例，统一发布产物命名。
