/**
 * @deprecated Import terminal orchestration from `features/terminal/useTerminal`.
 * This module remains to avoid breaking existing renderer integrations.
 */
export {
  useTerminal,
  type TerminalConnection,
  type UseTerminalOptions,
  type UseTerminalReturn
} from '../features/terminal/useTerminal'
