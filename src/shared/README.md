# Shared

主进程、预加载脚本和渲染进程共享的纯 TypeScript 类型、常量和契约。

## 约束

- 不导入 `electron`、`vue`、Node.js 内置模块或任意 UI 组件。
- 不含运行时副作用。
- `ipc` 定义请求、响应和事件的单一类型来源。
- `settings` 定义基础设置的模型与版本信息。
- `workbench` 定义通用工作台的 Tab、面板和布局模型；不得包含连接、主机、串口、终端等业务字段。
